# 작업 목록: 뉴스 큐레이션 서비스 (News Curation Service)

**입력**: `docs/spec/`의 설계 문서들
**필수 조건**: [plan.md](plan.md), [specification.md](specification.md)

## 1단계: 설정 (공통 인프라)

**목적**: 프로젝트 초기화 및 기본 구조 설정

- [x] T001 구현 계획에 따른 프로젝트 구조 생성 (기존 Next.js 구조)
- [x] T002 Next.js 의존성을 포함한 TypeScript 프로젝트 초기화
- [x] T003 린팅(Linting) 및 포맷팅(Formatting) 도구 설정

---

## 2단계: 기반 작업 (차단성 필수 조건)

**목적**: 어떠한 사용자 스토리도 구현되기 전에 반드시 완료되어야 하는 핵심 인프라

- [x] T004 SQLite 데이터베이스 설정 (`src/infrastructure/database/sqlite.ts`)
- [x] T005 SystemSettingRepository 구현
- [x] T006 NewsRepository 설정
- [x] T007 `src/domain/entities`에 Category 및 News 엔티티 정의
- [x] T008 NewsAPI 및 OpenAI 클라이언트 설정

---

## 3단계: 사용자 스토리 1 - 최신 뉴스 피드 조회 (우선순위: P1) 🎯 MVP

**목표**: 사용자는 AI 요약이 포함된 최신 뉴스 피드를 볼 수 있다.
**독립 테스트**: `/` 방문 후 뉴스 카드 확인.

### 구현 (Implementation)
- [x] T011 [US1] `News` 엔티티 모델 생성
- [x] T012 [US1] NewsAPI에서 뉴스를 가져오는 `NewsFetchService` 구현
- [x] T013 [US1] GPT 요약을 위한 `AISummaryService` 구현
- [x] T014 [US1] 페이지네이션이 포함된 `GET /api/news` 엔드포인트 생성
- [x] T015 [US1] `NewsCard` 컴포넌트 구현
- [x] T016 [US1] 무한 스크롤이 포함된 `NewsFeed` 컴포넌트 구현
- [x] T017 [US1] [FR-008] 드롭다운 메뉴 간격 수정 (버그 수정)
    - [x] `CategoryTabs.module.css` 분석
    - [x] 투명 브릿지 가상 요소(pseudo-element) 구현
    - [x] 수정 사항 검증

---

## 4단계: 사용자 스토리 2 - 카테고리별 뉴스 필터링 (우선순위: P2)

**목표**: 사용자는 기술/과학 카테고리별로 뉴스를 필터링할 수 있다.
**독립 테스트**: 카테고리 탭 클릭 후 필터링 동작 확인.

### 구현 (Implementation)
- [x] T021 [US2] `categoryId` 필터링을 지원하도록 `NewsRepository` 업데이트
- [x] T022 [US2] `categoryId` 파라미터를 허용하도록 `GET /api/news` 업데이트
- [x] T023 [US2] `CategoryTabs` 컴포넌트 구현
- [x] T024 [US2] 홈 페이지에 카테고리 선택 기능 통합

---

## 5단계: 사용자 스토리 3 - 관리자 설정 (우선순위: P3)

**목표**: 관리자는 카테고리와 스케줄을 관리할 수 있다.
**독립 테스트**: `/admin` 방문 후 카테고리 추가 및 스케줄 변경 확인.

### 구현 (Implementation)
- [x] T031 [US3] `GET /api/categories` 및 `POST /api/categories` 생성
- [x] T032 [US3] `POST /api/news/fetch` 트리거 엔드포인트 생성
- [x] T033 [US3] 관리자 대시보드 페이지 구현 (`/admin`)
- [x] T034 [US3] node-cron을 이용한 `SchedulerService` 구현

---

## 6단계: 다듬기 및 횡단 관심사

**목적**: 여러 사용자 스토리에 영향을 미치는 개선 사항

- [x] T101 `docs/` 내 문서 업데이트 (`ARCHITECTURE`, `API` 등)
- [x] T102 코드 정리 (`CategoryTabs` CSS 수정은 US1에 포함됨)
